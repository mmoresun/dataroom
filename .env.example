VITE_API_URL=http://localhost:3001/api/v1
# Optional — only needed to test Google sign-in locally. Must be the SAME OAuth client
# whose ID/secret you put in backend/.env's GOOGLE_CLIENT_ID/GOOGLE_CLIENT_SECRET.
# See README.md's "Local Development Setup" for how to create one.
VITE_GOOGLE_CLIENT_ID=
